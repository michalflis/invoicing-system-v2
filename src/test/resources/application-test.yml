invoicing-system:
  database: jpa

spring:
  datasource:
    url: jdbc:h2:mem:db-jpa;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  flyway:
    enabled: false
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect



