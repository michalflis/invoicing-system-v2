invoicing-system:
  database: jpa

spring:
  datasource:
    url: jdbc:h2:mem:db-jpa;
    username: sa
    password:
    driver-class-name: org.h2.Driver
  flyway:
    enabled: false
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect



